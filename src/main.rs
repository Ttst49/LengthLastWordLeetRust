fn length_of_last_word(s: String) -> i32 {
    let mut len = 0i32;
    for as_byte in s.as_bytes().iter().rev() {
        
    }
}


fn main() {
    let sentence = String::from("Luffy is still JoyBoy");
    length_of_last_word(sentence);
}
